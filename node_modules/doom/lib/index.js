(function(){
  var merge;
  merge = function(xs){
    var result, i$, len$, source;
    result = {};
    for (i$ = 0, len$ = xs.length; i$ < len$; ++i$) {
      source = xs[i$];
      import$(result, source);
    }
    return result;
  };
  module.exports = function(engine, events){
    return merge([require('./collection'), require('./traversing')(engine), require('./query')(engine), require('./event')(events, engine), require('./manipulation'), require('./property'), require('./presentation')]);
  };
  function import$(obj, src){
    var own = {}.hasOwnProperty;
    for (var key in src) if (own.call(src, key)) obj[key] = src[key];
    return obj;
  }
}).call(this);
